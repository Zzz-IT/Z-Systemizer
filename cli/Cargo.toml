[package]
name = "systemizer"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Safe Rust executor for KSU Systemizer"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
