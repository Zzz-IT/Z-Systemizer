[package]
name = "systemizer"
version = "1.1.3"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Safe Rust executor for KSU Systemizer"

[profile.release]
lto = true
codegen-units = 1
strip = true
panic = "abort"

[dependencies]
